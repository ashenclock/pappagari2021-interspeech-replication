# Core dependencies
torch>=2.0.0
transformers>=4.30.0
scikit-learn>=1.2.0
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
pydantic>=1.10.0

# ASR backends (install based on your needs)
# For Parakeet (NeMo):
# nemo_toolkit[asr]>=1.20.0

# For WhisperX:
# whisperx>=3.1.0

# For faster-whisper (alternative):
# faster-whisper>=0.9.0

# Optional but recommended
# tensorboard>=2.13.0  # for logging
# wandb>=0.15.0        # for experiment tracking
